#!/usr/bin/env bash
set -euo pipefail

repo_root="$(git rev-parse --show-toplevel)"
cd "$repo_root"

# pull --rebase dispara post-rewrite; en ese caso también incrementamos build.
if [[ "${1:-}" == "rebase" ]]; then
  node scripts/bump-build-on-pull.js
fi
