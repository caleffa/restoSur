window.__RUNTIME_CONFIG__ = {
  // URL del backend REST (incluye /api).
  VITE_API_URL: 'https://localhost:3000/api',
  // URL websocket de cocina (opcional).
  VITE_KITCHEN_WS_URL: 'wss://localhost:3000/ws/kitchen',
};
